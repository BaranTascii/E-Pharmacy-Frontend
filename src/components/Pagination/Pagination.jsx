const Pagination = () => {
  return (
    <div className="pagination">
      <button>{"<"}</button>

      <button className="active-page">1</button>

      <button>{">"}</button>
    </div>
  );
};

export default Pagination;
